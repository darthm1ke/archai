[ids]
*

[main]
# Caps Lock — primary PTT key (bare metal). Suppress toggle, keep LED feedback.
capslock = noop
# Right Ctrl — VM/QEMU fallback PTT key. Left as-is so it passes through.
# rightcontrol = noop  (intentionally NOT suppressed)
