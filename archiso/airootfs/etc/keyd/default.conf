[ids]
*

[main]
# Caps Lock is owned by ArchSpeech push-to-talk.
# Suppress the default toggle so it acts as a pure PTT key.
# The LED still lights up naturally on keydown — mic active indicator.
capslock = noop
