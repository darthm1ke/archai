# Build output
build/
work/

# Package and model caches (too large for git — run scripts/fetch-deps.sh)
pkg-cache/
archiso/airootfs/usr/local/lib/archspeech/models/*.gguf
archiso/airootfs/usr/local/lib/archspeech/wheels/

# Secrets (only ignore if it contains real API keys — default config is safe to commit)

# Test VM disk and logs
archai-test.qcow2
aios-boot.log

# Ollama model blobs — too large for GitHub (>100MB), staged by fetch-deps.sh
archiso/airootfs/usr/local/lib/archspeech/ollama/
archiso/airootfs/root/.cache/

# OS
.DS_Store
Thumbs.db
